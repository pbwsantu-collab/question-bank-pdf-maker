/**
 * jsPDF is loaded from CDN for this repository to keep the repo lightweight.
 * The Service Worker caches https://cdnjs.cloudflare.com/ajax/libs/jspdf/2.5.1/jspdf.umd.min.js
 * so the app works offline after the first successful load.
 *
 * For a fully self-contained offline package (no network ever needed after download),
 * use the zip artifact which includes the local jspdf.umd.min.js file, or download it from:
 * https://cdnjs.cloudflare.com/ajax/libs/jspdf/2.5.1/jspdf.umd.min.js
 * and place it next to index.html, then change the script src back to "jspdf.umd.min.js".
 */
console.warn("jsPDF loaded via CDN + Service Worker cache. See README.");
